# ─────────────────────────────────────────────────────────
# Lotus Kindergarten — Environment Variables
# Copy this file to .env and fill in your values.
# On Render/Railway these are set in the dashboard, not here.
# ─────────────────────────────────────────────────────────

# Environment
NODE_ENV=production

# Port (Render sets this automatically — do not override)
# PORT=8000

# MongoDB Atlas connection string
# Get from: https://cloud.mongodb.com  →  Connect  →  Drivers
DATABASE_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/lotus-kindergarten?retryWrites=true&w=majority

# JWT — use a long random string (min 32 chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-secret-string
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# CORS — your Render frontend URL (e.g. https://lotus-kindergarten.onrender.com)
FRONTEND_URL=https://your-app-name.onrender.com

# Email (optional — for password-reset emails, uses nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=Lotus Kindergarten <no-reply@your-domain.com>

# Cloudinary (optional — for image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
